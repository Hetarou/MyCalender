# このリポジトリで作業するAIへ

個人用のスケジュール管理カレンダー。作者は大学生で、自分の時間割・予定・課題の締切を1画面で見るために使う。

## ファイル構成

| ファイル | 中身 |
|---|---|
| `index.html`（または `calendar.html`） | アプリ本体。これ1つで動く |
| `AGENTS.md` | このファイル。作業のきまり |
| `STATUS.md` | **いまの状態と決めた設計方針。着手前に必ず読む** |
| `SETUP.md` | 公開・アプリ化・Googleカレンダー連携の手順 |
| `test.js` | 検証用。`node test.js` |
| `manifest.json` / `icon-*.png` / `apple-touch-icon.png` | ホーム画面やデスクトップに入れるための設定とアイコン |
| `README.md` | 最初に読む案内 |
| `smoke.js` | 画面ごと動かして確かめる補助（任意。jsdomが要る） |

`manifest.json` とアイコンは**あればアプリとして入れられる**という追加。
無くても `index.html` は単体で動く。**この関係を逆にしないこと**（本体が
これらのファイルに依存するようにしない）。

`AGENTS.md` は「どう書くか」、`STATUS.md` は「いま何をやるか」。両方読んでから着手する。

## 大原則

**`calendar.html` 1ファイルで完結させる。** 依存ライブラリなし、ビルド工程なし、フレームワークなし。
ブラウザでこのファイルを直接開けば動く状態を絶対に崩さないこと。

- npm / package.json を作らない
- React・Vue・Tailwind 等を導入しない
- CDNから何か読み込まない（オフラインで動かなくなる）

将来デスクトップアプリ化（Tauri か Electron）する予定があり、そのとき「HTMLを1枚渡すだけ」で済むのがこの構成の狙い。

**現在およそ2150行。1ファイルのまま進める方針。**
分割すると差分が複数ファイルに散ってレビューしづらくなり、AIに渡すときも手間が増える。
章立ては先頭のコメントにあるので、そこを頼りに目的の場所へ飛べばよい。
3000行を超えたら分割を相談する。

## 動かし方・確認のしかた

**変更したら、必ずこれを通してから出すこと。**

```bash
node test.js
```

失敗が1件でもあれば終了コードが1になる。目視では確かめにくい部分（日付のズレ、帯の段組み、
時間軸の重なり、Googleカレンダーとの相互変換）を検証している。

`test.js` は `index.html` / `calendar.html` の中身を**そのまま読み込んで**動かしている。
ロジックをコピーして持っているわけではないので、本体を直せばテストも同じコードを見る。

### 要素の取りこぼしに注意

`getElementById` が1つでも `null` を返すと、その行で処理が止まり、**以降が丸ごと動かなくなる**。
画面上は「その機能が無い」ように見えるだけで、エラーも出ないので気づきにくい。

`test.js` がJS側の参照IDとHTML側のIDを機械的に照合している。
**要素を消したり名前を変えたりしたら、必ずテストを通すこと。**

### 画面まわりを本当に動かして確かめたいとき

`test.js` は画面を見ない。DOMごと動かして確かめたい場合は jsdom を使う。

```bash
npm install jsdom
```

`url: 'https://example.com/'` を指定すること。省くと `about:blank` になり、
保存領域が使えず「保存できない」状態を再現してしまう（アプリのバグではない）。

### テストを足すとき

- ロジックを変えたら、その挙動を確かめるテストも一緒に足す
- **「わざと壊すと落ちること」を確認してから出す。** 壊しても通るテストは無いのと同じ
- 見た目（CSS・レイアウト）は対象外。ブラウザで目視確認する

### 表示だけ直したとき

`test.js` は画面を見ないので、CSSだけの変更は通って当然。実際にブラウザで開いて確認すること。

## データの形

配列 `items` にすべて入っている。種別は3つ。

```js
// 授業（毎週くり返す）
{ id, kind:'class', title, memo, weekday, start, end, termEnd }
//   weekday: 0=日 … 6=土（JSのgetDay()と同じ）
//   start/end: 'HH:MM'。授業も実時刻で持つ。時限番号では持たない
//   termEnd: 'YYYY-MM-DD' | null（nullなら期限なし）

// 予定（日をまたげる）
{ id, kind:'event', title, memo, date, endDate, start, end }
//   date    : 開始日 'YYYY-MM-DD'
//   endDate : 終了日。dateと同じなら1日で終わる
//   start/end: 'HH:MM' | ''（空なら終日あつかい）

// タスク（期限のあるやること）
{ id, kind:'task', title, memo, date, endDate, start, end, done, today }
//   date    : 期限。endDate は常に date と同じ。end は常に ''
//   start   : 任意（「23:59まで」のような期限時刻）
//   done    : 完了したか
//   today   : 「今日やる」に選んだ日 'YYYY-MM-DD' | ''
//   ※ 旧名 'due' は migrate() と fromGoogle() が 'task' に変換する

// today を真偽値にしないこと。
// 日付で持つことで「日付が変われば自動で外れる」が成立し、
// 毎日リセットするための書き込みが要らなくなる。判定は必ず isPicked() を通す。
```

### 触るときの注意

- **`endDate` が無い古いデータが存在しうる。** 必ず `endOf(it)` 経由で読むこと（`it.endDate || it.date` を返す）。`it.endDate` を直接参照しない
- **`period`（時限番号）は廃止済み。** 読み込み時に `migrate()` が `start`/`end` へ変換する。新しいコードから `it.period` を参照しないこと。フォームの「時限」ボタンは時刻を埋めるだけの入力補助で、保存はされない
- `id` は既存アイテムで絶対に変えない
- 日付は文字列 `'YYYY-MM-DD'` のまま比較する（辞書順＝時系列順になる）。Dateオブジェクトに変換して比較しない

## 画面の作り

### 画面はカレンダーとタスクで分かれている

`state.screen` が `'calendar'` か `'task'`。**この分離は意図的な設計。**

カレンダーに出すのは「その時刻でなければならないもの」＝**授業と予定だけ**。
タスクはタスク画面にしか出ない。やらなかった予定が積み重なるとカレンダー自体が
信用されなくなるため（GTDのハードランドスケープの考え方）。

- **`itemsOn()` は `kind === 'task'` を返さない。** ここにタスクを混ぜないこと
- **`barItems()` は `kind === 'event'` のみ。** 授業とタスクは対象外
- 右レーン（`renderRail()`）は**一覧ではなく「タスク画面への窓」**。
  「今日やる」を選んでいればそれを、選んでいなければ期限の近い3件を出す。
  ここにタスク一覧を展開しないこと。分離の意味が消える
- タスク画面では、**同じタスクを2箇所に出さない。** 「今日やる」に選ばれたものは
  「期限切れ」「これから」から外す

### 週ビュー（既定）

```
[曜日ヘッダー]
[帯（band）]      ← 日またぎ・終日の予定、締切
[時間軸（hours）]  ← 1時間刻み。時刻つきの予定を絶対配置で置く
```

振り分けは2つの関数だけが決める。**条件をインラインに書き散らさないこと**（テストから
本物を呼べなくなり、片方だけ壊れても気づけなくなる）。

- `barItems(cols)` … 帯に出すもの。予定のうち、日をまたぐ / 開始時刻が空のもの
- `gridItems(date)` … 時間軸に出すもの。**授業**と、その日で終わる時刻つきの予定

**同じ予定を両方に出さないこと。** タスクはどちらにも出ない。
この排他はテストで検査している（授業を含めて全種別を対象にすること）。

時間軸は `.hours` の中に、時刻ラベル列 `.tcol` と各曜日の列 `.dcol` が並ぶ。
`.dcol` は `position:relative` で、中の `.ev`（予定）を `top` / `height` / `left` / `width` で絶対配置している。
位置の計算式は `minTop()` と `evHtml()` にまとまっているので、**px計算をあちこちに散らさないこと**。

同じ時間帯に複数の予定があるときは `layoutDay()` が左右に分ける。
重なりの「かたまり」ごとに列数を数えて、かたまり内で幅を等分する方式。
表示範囲の外（`DAY_START` より前 / `DAY_END` より後）にはみ出す予定は端に寄せて、必ず見える・押せる状態を保つこと。

### 月ビュー

1週間ごとに `.mweek` という行を作り、その中に
- 7個の `.mcell`（日付＋そのセル固有の予定）
- `.mbars`（絶対配置のグリッド。帯がセルをまたいで伸びる）

を重ねている。`.mcell` の中には `.mspace` という空きブロックがあり、帯の段数ぶんの高さを確保している。**帯の段数を変えたらこの高さも一緒に直すこと**（`lanes * 20px`）。

### 帯の段組み `layoutBand()`

同じ日に重なる帯どうしが同じ段に来ないよう、上から順に空いている段へ置く。
週をはみ出す帯には `cutL` / `cutR` が立ち、その側の角丸を消して「続いている」ことを表す。
1段 = 20px（バー18px + 上下マージン1px）で、CSSの `grid-auto-rows:20px` と揃えてある。**片方だけ変えない。**

なお `layoutBand()`（帯・日単位）と `layoutDay()`（時間軸・分単位）は別物。名前が似ているので取り違えないこと。

## 保存

保存先は2つあり、どちらも `list` / `upsert` / `remove` の3つを持つ。
アプリ本体はどちらを使っているか意識しない。**この3つ以外から保存処理を書かないこと。**

- `LocalBackend` … この端末の中だけ。同期しない
- `GoogleBackend` … Googleカレンダーが本体。どの端末からでも同じ予定が見える

いま使っている方が変数 `Backend` に入っている。切り替えは `useBackend()`。
保存先を増やすときは、同じ3つの関数を持つオブジェクトを足す。

### Google連携で触ってはいけないところ

- **`toGoogle()` / `fromGoogle()` は往復できる状態を保つこと。** 変換を足したら、全種別（授業・予定・日またぎ・終日・締切）で「変換 → 逆変換 → 元と一致」をテストしてから出す
- **`singleEvents=false` を `true` に変えない。** trueにするとくり返し予定が1回ずつに展開され、授業が壊れる
- **タスクは終日イベント + `extendedProperties.private` で表している。** 0分のイベントはGoogleが受け付けないため。期限時刻は `dueTime`、完了状態は `done` に入れる。この表現を変えると既存データが読めなくなる
- **`CONFIG.taskCalendarId` が入っているとタスクだけ別カレンダーに保存される。** 読み書き先の判定は `GoogleBackend.calIdFor()` に集約してあるので、カレンダーIDを他の場所で組み立てないこと
- Googleの終日イベントの `end` は「終わった翌日」を指す。1日ずれるバグの温床なので、`toGoogle`/`fromGoogle` の外で日付計算をしない
- **クライアントシークレットをコードに書かない。** ブラウザで動くので誰でも読める。`CONFIG.googleClientId` だけでよい
- `CONFIG.googleClientId` は作者ごとの値。**空文字にリセットしない**し、勝手に値を入れない

### 保存できない環境がある

アプリ内ブラウザ（LINEやXのリンクから開いた画面）やプライベートブラウズでは、
保存領域が使えず書き込みが失敗することがある。

- **起動時に `probeStorage()` で実際に書いて読み直して確かめている。** この確認を消さないこと
- 書き込みの失敗を握りつぶさない。失敗したら警告を出し、その項目に `_unsaved` を立てる
- エラー文言には「何が起きたか」だけでなく「どうすれば直るか」を書く

### 未実装（やるなら相談してから）

- 差分同期（`syncToken`）。いまは毎回まとめて取り直している
- オフライン時の書き込みキュー。いまは通信できないとエラーを出して保存しない
- 他端末の変更のリアルタイム反映。いまは再読み込みが必要

## 個人設定

どれも作者の生活に合わせた値。**勝手に「一般的な値」に直さないこと。**

- `PERIODS` … 1限〜6限の開始・終了時刻。フォームの「時限」ボタンが使う
- `DAY_START` / `DAY_END` … 週ビューで表示する時間帯
- `HOUR_PX` … 1時間ぶんの高さ(px)

**`HOUR_PX` を変えるときは CSS 側と食い違わないか確認すること。** 予定の高さはこの定数から計算しているので、CSSに直接pxを書き足すとズレる。

## 描画のきまり

体感速度に直結するので、次の3つは崩さないこと。

- **保存を待ってから画面を更新しない。** 保存ボタンを押したら先に `render()` し、書き込みは `persist()` が裏で走らせる。失敗したら警告を出して、その予定に `_unsaved` を立てて破線で見せる
- **クリックのイベントは要素に貼らない。** `#grid` と `#rail` に1つずつ置いた委譲だけを使う（`initClicks()`）。再描画のたびに貼り直すと、要素数に比例して重くなる
- **時間軸の罫線をDOMで作らない。** `.dcol` のCSS背景で描いている。要素で描くと1描画あたり200個以上増える

`HOUR_PX` はJS側の値を `.hours` の `--hour` に流し込んでCSSが使っている。**どちらか片方だけ変えない。**

## 名前のつけかた

**実体と合わない名前を残さないこと。** 次のAIが誤解する。

過去にあった例：締切(`due`)をタスク(`task`)に統合したあとも、右レーンのCSSクラスが
`.due-item` のままだった。いまは `.rail-item`。仕様を変えたら名前も一緒に直す。

ただし**古いデータを読むための互換コードは別**。`fromGoogle()` と `migrate()` にある
`'due'` の扱いは、前の版で保存したデータを読むために必要。消さないこと。

## デザインを変えるとき

見た目はどんどん変えていい。ただし**CSSとJSが同じ数値を共有している箇所**があり、
片方だけ変えるとズレる。変える場合は必ず両方直す。

| 数値 | JS側 | CSS側 |
|---|---|---|
| 1時間の高さ | `HOUR_PX` | `.hours` の `--hour`（JSから流し込まれる） |
| 帯1段の高さ | `lanes * 20px`（`.mspace` の高さ） | `.band` / `.mbars` の `grid-auto-rows:20px` |
| 月ビューの帯の開始位置 | — | `.mbars` の `top:24px` = `.mcell` の padding 4px + `.n` の 18px + gap 2px |
| 週グリッドの列数 | 帯の `grid-column:${i+2}` | `.week` の `34px repeat(7,1fr)` |

`.mcell .n` の高さや `.mcell` の padding を変えたら、`.mbars` の `top` も一緒に直す。
これを忘れると、月ビューで帯だけが数px浮く。

### 消してはいけない指定

- `.dcol` / `.mweek` の `position:relative` … 中の予定を絶対配置で置いているため
- `[hidden]{display:none!important}` … これが無いとフォームの項目が隠れなくなる
- `esc()` を通さずに予定のタイトルを画面に出さない … 他人から共有された予定に仕込まれる可能性がある

### background の短縮形に注意

`.dcol` の時間軸の罫線は**CSSの背景画像**で描いている。
ここに `background: 色` と短縮形で書くと `background-image` まで消えて、**罫線が消える。**
色だけ変えたいときは必ず `background-color` を使う。

```css
.dcol:hover{ background-color:#F4F7F9 }   /* ✅ */
.dcol:hover{ background:#F4F7F9 }         /* ❌ 罫線が消える */
```

### 緊急度は色で表さない

**色相はすべて種類（カテゴリ）に明け渡してある。** 緊急度は次の2つで表す。

- **まとまり** … 期限切れ / 今日・明日 / 今週 / それ以降 に分けて並べる
- **濃さと太さ** … `lv0`（濃い・太い）→ `lv1` → `lv2`（薄い）。色相は使わない

**取り消し線は「完了」だけの意味。** 期限切れに使わないこと。
期限切れは未完了で、しかも一番目立たせるべきものなので、逆の印になる。

緑（`--today`）だけは「今日・完了」で使い続けているので、種類の色をここに近づけない。
`test.js` が検査している。

### 種類（カテゴリ）

`CONFIG.categories` は**初期値**。実際に使う一覧は変数 `cats` で、
アプリの「種類を編集」から書き換えられ、保存先に保存される。

- 項目側は `cat`（idの文字列）だけを持つ。**名前や色を持たせないこと。**
  名前や色を変えたときに登録済みの予定を書き換える羽目になる
- **idは中で作って外に出さない。** 名前を変えてもidは変えない
- 種類を消したら、その予定は未分類に落ちる（`catOf()` が拾う）。予定自体は消さない
- 保存先ごとの持ち方
  - `LocalBackend` … `cal:cats` に置く
  - `GoogleBackend` … 目印 `jikanwariSettings=1` を付けた1件の予定にJSONで入れる。
    端末をまたいで揃うようにするため。この予定は `fromGoogle()` が弾いて画面に出さない
- **タスク行を色で塗らないこと。** カテゴリはタイトル前の小さい点で示す
- 未分類（`cat` が空）のときはCSS変数を出力せず、既定の見た目に落ちるようにしてある

### 期限の表し方

`dueLabel()` に集約してある。「D-2」のような書き方は分かりにくいので使わない。
「今日」「明日」「あと5日」「2日すぎ」のように普通の日本語で出し、数字だけ大きくする。

### 色の意味は変えない

3色それぞれに意味を1つだけ割り当ててある。装飾目的で使い回すと情報が失われる。
色そのもの（何色にするか）は自由に変えてよい。

## CSS

- 色は `:root` のCSS変数のみ使う。生の16進数をその場に書かない
- 色には意味を1つずつ割り当ててある。**混ぜないこと**
  - `--plan`（青）＝ 予定・授業
  - `--due`（赤）＝ 締切のみ
  - `--today`（緑）＝ 今日のみ
- 日本語フォントは端末標準を使う（Webフォントを読み込まない）
- **`hidden` 属性を使う要素に `display` を指定しない。** 自作CSSがブラウザ標準の `[hidden]{display:none}` に勝ってしまい、消えなくなる。対策として `[hidden]{display:none!important}` を入れてあるが、そもそも踏まないほうがいい

## 文言

- 作者向けの個人ツールなので、話し言葉まじりのくだけた日本語でよい
- エラーは謝らない。何が起きたかと、どうすれば直るかを書く
  - 良い例：「終了時刻が開始時刻より前になってる。日をまたぐ予定なら、終了日を翌日にしてね。」
  - 悪い例：「申し訳ありません。入力に誤りがあります。」

## これから

やる順に並べてある。上から進める想定。

1. 静的ホスティングに置く ← **完了**
2. Googleカレンダー連携を有効にする（作者がクライアントIDを入れる） ← 次
3. 使ってみて出た不満を潰す
4. Tauri か Electron でデスクトップアプリ化

**いまの詳しい状況と、着手してよいタスクは `STATUS.md` にある。**

**1と2の順番は入れ替えられない。** GoogleのOAuthは接続元のURLを事前登録する仕組みなので、
本物のURLが無いと連携の設定ができない。ローカルで試す場合も `file://` ではなく
`http://localhost:8000` などで開く必要がある。
