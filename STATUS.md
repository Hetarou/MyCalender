# いまどこにいるか / 次に何をやるか

作業を始める前にこれを読む。書き方のルールは `AGENTS.md`、外部サービスの設定は `SETUP.md`。

---

## 現在地

**公開済み。Googleカレンダーと接続して稼働中。実データが入っている段階。**

| | 状態 |
|---|---|
| アプリ本体 | 動いている（約3000行の1ファイル） |
| 公開 | GitHub Pages で公開済み |
| アプリとしての導入（PWA） | 対応済み（`manifest.json` + アイコン3種） |
| カレンダー | 今日 / 週 / 月。画面幅でPCは週、スマホは今日ビューが既定 |
| タスク管理 | タスク画面・完了チェック・「今日やる」 |
| 種類（カテゴリ）と色 | アプリ内で編集でき、保存先に保存される |
| 設定 | 独立した設定画面に集約（同期・表示・時限・種類・データ） |
| テーマ | 紙（明るい）／夜（暗い）を切り替えられる。既定は紙 |
| 保存先 | **`GoogleBackend`（端末間で同期）** |
| Googleカレンダー連携 | **接続済み・稼働中** |
| 実データ | Googleカレンダー側にある。**壊すと実害が出る** |
| アプリ名 | `schedule` |
| テスト | 65件 |

### Google連携は稼働中

作者がクライアントIDを取得済みで、保存先はGoogleカレンダーになっている。

- **`CONFIG.googleClientId` を変更・削除しないこと。** 作者のGoogleアカウントに紐づく値
- **本物のカレンダーを直接書き換える。** アプリで予定を消すとGoogleカレンダーからも消える
- `toGoogle()` / `fromGoogle()` を触るときは特に慎重に。日付が1日ずれるだけで実害が出る
- 大きめの改造をするときは、Googleカレンダーで実験用カレンダーを作り、
  開発中だけ `CONFIG.calendarId` をそちらへ向ける（手順は `SETUP.md`）

### 動作確認はどこでやるか

**公開したURLで確認すること。** Claudeのアーティファクトのプレビュー内では
保存操作が成功しないため、保存まわりの検証ができない（アプリはこれを検出して警告を出す）。

ブラウザが古いファイルを覚えていることがあるので、確認時はURLの末尾に `?v=2` などを付ける。

---


## 2026-07-24 UI刷新（実装済み）

この節は、以前のレイアウトに関する記述より新しく、食い違う場合はこちらを優先する。

- **大型PC（1180px以上）**：幅208pxの左サイドバー。カレンダー／今日／タスク／設定
- **小型PC・タブレット**：上部ナビゲーション。週ビューは維持
- **スマートフォン（767px以下）**：下部ナビゲーション。今日／カレンダー／タスク／設定
- **PCのタスク画面**：「今日やる」と「すべてのタスク」の2ペイン
- **スマホのタスク画面**：同じ内容を縦に並べる
- **設定**：ダイアログではなく独立画面。テーマはPaper／Nightのプレビュー選択
- **追加・編集**：PCは右側シート、スマホは下から開くほぼ全画面シート
- **予定カード**：カテゴリ色の細い左線＋薄い背景。高さが足りる場合はメモ先頭行（場所など）も表示。強い影や発光は使わない
- **操作の手触り**：空き時間の時刻ガイド、予定ホバー、軽い押し込み、方向付き画面遷移、完了アニメーション
- **スマホの操作**：タップ反応、下部ナビの選択表示、保存トースト、対応環境のみ短い振動
- **設定の安全性**：カテゴリは上下に並び替え可能。時限は終了時刻が開始時刻より後か検証。データ読込前に確認を表示
- **初回刷新では入れない**：タスク横スワイプ、ドラッグ移動、ドラッグ時間変更、長押しメニュー

実装後の確認：`node test.js` は **65件成功 / 0件失敗**。PC 1440px、タブレット 1024px、スマホ 390pxで表示と主要操作を確認済み。確認には隔離したサンプルデータを使い、実際のGoogleカレンダーへの追加・更新・削除は行っていない。

---
## これまでに決めた設計方針

**変更するときは作者に確認すること。** 理由まで含めて記録してある。

### 1. カレンダーとタスクは別画面にする

カレンダーに出すのは「**その時刻でなければならないもの**」＝授業と予定だけ。
タスクはタスク画面にしか出さない。

理由：やらなかった予定が積み重なると、カレンダー自体が信用されなくなる。
本物の予定（授業・約束）まで見なくなるのを避けるため（GTDのハードランドスケープの考え方）。

**カレンダー画面にタスク欄は無い。** 以前は右側にあったが撤去済み。
残タスクの存在は、上部の「タスク」タブの数字バッジで分かる。
唯一の例外は**今日ビューの中の「今日やる」**（予定とタスクを日単位で管理するため）。

### 2. タスクの保存先はGoogleカレンダー。Google ToDo は使わない

Google ToDo のほうがデータモデルは合っているが、採用しない。

- **期限に時刻を保持できない**（APIが時刻部分を破棄する）。「23:59提出」が表現できなくなる
- 認証と同期処理がもう1本増える

作者の判断で不採用。**再提案しないこと。**

### 3. タスクには必ず期限がある

日付なしタスクは持たない。カレンダーの予定として保存する以上、日付が必須のため。

「使ってみて本当に困ったら考える」という保留状態。**先回りして実装しないこと。**

### 4. 緊急度は色で表さない

色相はすべて種類（カテゴリ）に使う。緊急度は「まとまり分け」と「濃さ・太さ」で表す。

色を2つの意味に使うと、赤が「緊急」なのか「バイト」なのか分からなくなるため。
**取り消し線は完了だけの意味**で、期限切れには使わない（逆の印になる）。

### 5. 見た目は読みやすさ最優先

**読みやすさと押しやすさが、見た目の格好よさより優先する。**

- 12px未満の文字を使わない
- 本文と地の明暗差を大きく取る
- 英字のラベルを使わない
- 飾り（斜線、角のL字、ID番号、進捗バー）を足さない
- 角を丸め、一覧は白いカードにまとめる

暗い書類様式、ネオン都市、余白を広げる案も試したうえでこの方向に決めた。
**過去の案に戻さないこと。**

### 6. テーマは切り替えられる。色の役割は共通

紙（明るい）／夜（暗い）。`THEMES` に定義し、CSSは `[data-theme="<id>"]` で上書きする。

**役割はテーマ間で共通**にする（`--accent`＝いま・今日・選択中、`--done`＝完了）。
役割まで変えると、切り替えるたびに意味を覚え直すことになる。

### 7. 「今日やる」は日付で持つ

`today: 'YYYY-MM-DD'`。真偽値にしない。
日付が変われば自動的に外れるので、毎日リセットするための書き込みが要らない。

### 8. 1ファイル完結を崩さない

`index.html` 単体でブラウザで開けば動く。ビルド工程なし、依存ライブラリなし。
`manifest.json` とアイコンは**あればアプリとして入れられる**という追加であって、
無くてもページは動く。

### 9. AIからの予定操作は、アプリに機能を足さない

Googleカレンダーを共通の置き場にして、AIにはそちらを操作させる。

```
AI  →  Googleカレンダー  ←  このアプリ
```

自前のAPIやMCPサーバーを作らない。Googleカレンダーは大抵のAIが標準で扱えるので、
そのほうが対応範囲が広く、常時動くサーバーも要らない。

**まだ足していない小さな変更**：`CONFIG.taskCalendarId` を設定している場合、
そのカレンダーの終日予定は隠し欄が無くてもタスクとして読む。
これでAIが普通に予定を作るだけでタスクになる。**着手前に作者へ確認すること。**

---

## 外部メモの精査結果

別セッションで作られた引き継ぎメモを作者と1件ずつ確認した。
**下記が確認済みの結論。メモ側の記述と食い違う場合はこちらが正。**

### 採用したもの

- **スマホ用の「今日ビュー」** … 実装済み。時間軸の表ではなくリスト
- **アクセストークンの保存** … 毎回認証を求められる問題への対処。実装済み
- **設定画面への集約** … 実装済み
- **同期ステータスの表示** … 独立した設定画面の中に実装済み

### 採用しないもの

- **「次の予定カード」を独立した部品として作ること** … 今日ビューの一部としてのみ存在する
- **大型PC限定の左サイドバー** … 2026-07-24のUI刷新で採用。1180px未満では上部ナビへ切り替える
- **右側に選択日詳細を常設** … カレンダー画面にタスク・詳細を常設しない方針と衝突する
- **プロジェクト / タグ / 分析 / フィルター** … 話に出ていない機能。画像を根拠に実装しないこと

### メモの記述が誤っていたもの

- **クイック追加は実装済み。** メモは未実装として書いているが、週ビューの14時あたりを
  押すと、日付と14:00–15:00 が入った状態でシートが開く。作り直さないこと
- **「暗いテーマに強く惹かれている」は言い過ぎ。** 作者は明るい方向を選んだうえで、
  「配色はまだ定まっていない」と述べている。だからテーマ切り替えを実装した
- **「角丸が多く柔らかすぎる」という違和感の記述は、作者の直近の指示と逆。**
  角の丸みは作者が3案から選んで採用したもの

### まだ確認が取れていないもの

**着手前に作者へ確認すること。**

- 検索機能
- ドラッグ操作（予定を動かして時刻変更など）
- 月ビューで1日に表示する予定の最大件数と「＋2件」表示
- 初回接続時のローカルデータ移行（実データを入れる前に接続できたため、不要になった可能性が高い）

---

## 慎重に進める段階

**Googleカレンダーに接続済みなので、失敗すると本物の予定に影響する。**

- ブランチを切って作業し、`node test.js` を通してからマージする
- 予定の変換（`toGoogle()` / `fromGoogle()`）を触るときは、往復のテストを必ず確認する
- 一度に複数の大きな変更をしない

---

## 作業の前後にやること

```bash
node test.js      # 65件。1件でも落ちたら直してから出す
```

そのうえで、**公開したURLをブラウザで開いて**次を目視確認する（`test.js` は画面を見ない）。

- カレンダー：今日 / 週 / 月をすべて開く
- **授業が週ビューの時間軸に出ているか**（過去に消えた実績がある）
- 予定を1件追加 → 編集 → 削除する
- タスク：追加 → 「今日やる」に上げる → 完了チェック → 完了済みに移る
- タスクがカレンダー側に出ていないこと
- 設定：テーマを切り替えて、両方で文字が読めること
- ウィンドウを狭くして、スマホ幅で崩れていないか見る

画面ごと動かして確かめたいときは、任意で `smoke.js` が使える（`npm install jsdom` が要る）。

ロジックを変えたなら、その挙動のテストも `test.js` に足す。
**足したテストは、わざと壊すと落ちることを確認してから出すこと。** 通るだけのテストは無いのと同じ。

---

## コードの整理状況

- **約3000行。1ファイルのまま進める方針。** これ以上大きな機能を足す前に分割を相談する
- 先頭のコメントに章立ての地図がある。目的の場所はそこから探す
- 今日ビューとタスク画面で重複していた組み立ては `catDot()` / `dateText()` /
  `urgency()` / `dueLabel()` に共通化した。**片方だけ直さないこと**
- 実体と合わない名前は直してある。ただし `fromGoogle()` と `migrate()` にある
  旧名 `'due'` の扱いは、前の版で保存したデータを読むために必要。**消さないこと**

### 過去に踏んだ罠（同じことを繰り返さないため）

- **`getElementById` が `null` を返すと、その行で処理が止まり以降が丸ごと動かない。**
  エラーも出ないので「その機能だけ無い」ように見える。`test.js` が参照IDを照合している
- **`background` の短縮形は `background-image` を消す。** `.dcol` の罫線が消える
- **`hidden` 属性は、自作の `display` 指定に負ける。** `[hidden]{display:none!important}` で対処済み
- **`#grid` の `z-index:0` を消すと、予定ブロックが他の要素より前に出る**
- **CSSを範囲で削除しない。** セレクタ単位で消すこと（過去に大量に消し飛ばした）

---

## 手をつけるとよいもの

上から順に、効果が大きくスコープが小さい。**1回のPRで1つだけ。**

### 1. 予定の簡易詳細パネル

いまは予定を押すといきなり編集画面が開く。確認したいだけのときに重い。
まず小さなパネルで内容（タイトル・時刻・メモ）を見せ、そこから［編集］［削除］へ進む形にする。

- 既存の `openSheet()` は残し、その手前に一枚はさむ
- タスク画面の行も同じ扱いにするかは、作ってから決める

### 2. タスクの並べ替え・絞り込み

件数が増えたときに効く。まずは「これから」の並び順を期限順以外にも選べるようにする程度で十分。

### 3. キーボード操作

- `←` / `→` で前後の日・週・月へ
- `t` で今日へ
- 入力欄にフォーカスがあるときは効かないようにする

---

## 相談してから着手するもの

以下は影響が大きい。**作者の確認を取ってから。**

- **くり返しの種類を増やす**（隔週・複数曜日・月1回など）
  → `toGoogle()` / `fromGoogle()` のRRULE変換を全部作り直すことになる。連携の要
- **「今日やる」に件数の上限を付ける** → 使ってみてから判断する、と決めてある
- **起動時にタスク画面を開く** → 同上
- **日付なしタスク** → 上の「決めた設計方針 3」を参照
- **オフライン対応（Service Worker）**
  → 更新しても古い画面が出続ける事故が起きやすい。入れるなら更新の仕組みまで設計する
- **通知** → Service Worker が前提。上と同じ理由で保留
- **差分同期・リアルタイム更新・コンフリクト解決** → 実運用してから検討する
- **ファイル分割** → 1ファイル完結は意図的な設計

---

## 渡し方

### チャット型のAI（ChatGPT / Gemini など）

この順で貼る。

1. `AGENTS.md`
2. このファイル（`STATUS.md`）
3. `index.html`

**順番が逆だとルールを知らないままコードを読み始める。**

### エージェント型（Claude Code / Codex / Cursor など）

リポジトリを渡せば `AGENTS.md` は自動で読まれる。Claude Code は `CLAUDE.md` を見るので、
`AGENTS.md` のコピーを置いてある。

### 頼み方の例

```
AGENTS.md と STATUS.md を読んでから作業して。
STATUS.md の「手をつけるとよいもの」の1番をやってほしい。

・変更は1つだけ。ついでに他を直さないこと
・node test.js を通してから出す
・何を変えたか、なぜそうしたかを簡潔に説明して
```

**「ついでに他も直さないで」は毎回入れる。** これが無いと、頼んでいない
リファクタやライブラリ導入が混ざってきて、差分が読めなくなる。

---

## 受け取ったものの確認

1. `node test.js` が通るか
2. 差分が頼んだ範囲に収まっているか（1ファイル完結なので差分は読みやすいはず）
3. 公開したURLで開いて、上の目視確認の項目を通るか
4. `AGENTS.md` の禁止事項を踏んでいないか
   - 外部からのスクリプト読み込みが増えていないか
   - `CONFIG.googleClientId` が書き換わっていないか
   - タスクがカレンダー側に出ていないか
   - 12px未満の文字が入っていないか
   - テーマを切り替えて両方とも読めるか

**怪しかったらマージせず、ブランチのまま置いておけばいい。** 1ファイルなので捨てるのも簡単。
