# Googleカレンダー連携のセットアップ

所要時間はだいたい30〜40分。**手順1を飛ばすと手順2ができない**ので、必ず上から順に。

理由：Googleは「どのURLからのアクセスを許可するか」を事前に登録させる仕組みになっている。
だから先に本物のURLを用意する必要がある。`file://` で開いたHTMLや、チャット内のプレビューでは通らない。

---

## 何が公開されて、何が公開されないか

先にここを整理しておく。**「リポジトリが公開」と「サイトが公開」は別の話。**

| | 公開される？ | 中身 |
|---|---|---|
| 予定のデータ | **されない** | Googleカレンダー（か、この端末のブラウザ）の中にある。リポジトリにもサイトにも入らない |
| コード（index.html） | リポジトリをpublicにすると見える | このアプリの見た目と動きだけ |
| クライアントID | 見える | 秘密情報ではない（下記） |
| クライアントシークレット | — | **使わない。書かない** |

**クライアントIDは隠せないし、隠す必要もない。**
ブラウザで動くアプリは、ログインのときにIDをGoogleへ送る。開発者ツールを開けば誰でも見える。
だからGoogleの設計上、IDは公開前提になっている。
守っているのは手順2-5で登録する**「承認済みのJavaScript生成元」**のほうで、
IDをコピーされても別のドメインから使おうとした時点で弾かれる。

**サイトのURL自体は、どの方法でも公開になる。**
ただし知らない人が開いても空のカレンダーが出るだけで、使うにはその人自身のGoogleアカウントで
ログインすることになる。表示されるのはその人自身の予定なので、こちらのデータは見えない。

### 唯一の注意点

**コードに実データを書かないこと。**
`init()` の中にあるサンプルデータの配列に、自分の本当の授業名や教室、バイト先などを
書いて commit すると、それは公開される。予定はアプリの画面から入力して、コードには書かない。

---

## 手順1：ネットに置いてURLを手に入れる

GitHub Pages が一番手軽。無料で、HTTPSが最初から付いてくる（Googleの連携にはHTTPSが必須）。

**ただし無料プランではリポジトリをpublicにする必要がある。**
コードを見られるのが嫌なら、下の「リポジトリを非公開にしたい場合」を見て。

1. GitHubで新しいリポジトリを作る（publicにする）
2. `calendar.html` を **`index.html` にリネーム**して push する
   - リネームしないと `https://.../calendar.html` まで打つことになる
3. リポジトリの Settings → Pages を開く
4. Source を `Deploy from a branch` にして、ブランチ（`main`）とフォルダ（`/ (root)`）を選んで Save
5. URLは待たなくてもわかる。この形になる

```
https://<GitHubのユーザー名>.github.io/<リポジトリ名>/
```

**このURLをスマホでも開いてみて、両方で表示されることを確認する。**

> ### 「Your site is live at 〜」が出てこないとき
>
> Saveの直後に出る `GitHub Pages source saved.` は、**保存できたという通知だけ**。
> 公開URLのバナーはこれとは別で、初回のビルドが終わってから出る。
> しかも**この画面は自動で更新されない**ので、待っていても変わらない。
>
> 1. Settings → Pages を**リロードする**（これだけで出ることが多い）
> 2. Actions タブで `pages build and deployment` を見る。緑なら完了、回っていれば待ち、赤ならビルド失敗
> 3. バナーを待たず、上のURLを直接開いてみる。ビルドさえ終わっていればアクセスできる
>
> 初回のビルドは5〜10分かかることもある。2回目以降は速い。
>
> **URLを開いて404（There isn't a GitHub Pages site here）が出たら、別の問題。**
> `index.html` がリポジトリ直下に無い。手順2のリネームを確認する。
この時点ではまだ同期しない（それぞれの端末に別々に保存される）が、それでいい。

### リポジトリを非公開にしたい場合

GitHub Pages のかわりに **Cloudflare Pages** か **Netlify** を使う。
どちらも無料枠でprivateリポジトリからデプロイできる。

1. サービスにGitHubアカウントでログイン
2. 「GitHubからインポート」で対象のリポジトリを選ぶ（privateのまま選べる）
3. ビルド設定は空でよい（ビルド工程が無いため）。出力フォルダはリポジトリ直下
4. デプロイすると `〜.pages.dev` / `〜.netlify.app` のURLがもらえる

以降の手順は同じ。手順2-5で登録するドメインを、そのURLのドメイン部分に読み替えるだけ。

なお**サイト自体はこの方法でも公開**になる。隠れるのはコードだけ。
サイトにもログインをかけたいなら Cloudflare Access（無料枠あり）を前段に置く手もあるが、
このアプリにはデータが入っていないので、普通はそこまでしなくていい。

> ### 手元で試したいとき
> `file://` で直接開くとGoogle連携が動かない。かわりにローカルサーバーを立てる。
> ```bash
> cd <calendar.htmlのあるフォルダ>
> python3 -m http.server 8000
> ```
> → `http://localhost:8000/calendar.html` で開ける。
> この場合、手順2-5の「JavaScript生成元」に `http://localhost:8000` も追加しておく。

---

## 手順1.5：アプリとして使えるようにする

ブラウザのタブから毎回探すのは面倒なので、**アイコンから直接開ける**ようにする。
Googleの設定より先にやってよい（手順2とは独立している）。

`manifest.json` と3つのアイコンがリポジトリ直下にあることが前提。
`index.html` と同じ場所に置いて push する。

```
index.html
manifest.json
icon-192.png
icon-512.png
apple-touch-icon.png
```

### iPhone（Safari）

1. 公開したURLを **Safariで** 開く（Chromeや他アプリ内のブラウザだと項目が出ない）
2. 下の共有ボタン（□に↑）を押す
3. 「**ホーム画面に追加**」を選ぶ
4. 名前を確認して「追加」

ホーム画面にアイコンができる。そこから開くと**アドレスバーが消えて全画面**になる。

### Android（Chrome）

1. 公開したURLをChromeで開く
2. 右上の「⋮」を押す
3. 「**アプリをインストール**」または「ホーム画面に追加」を選ぶ

### PC（Chrome / Edge）

1. 公開したURLを開く
2. アドレスバーの右端に出る**インストールのアイコン**を押す
   （出ないときはメニューの「保存して共有」→「ページをアプリとしてインストール」）
3. 独立したウィンドウで開くようになる。タスクバーやDockにも置ける

### 注意

- **オフラインでは開けない。** 通信が要る。オフライン対応（Service Worker）は、
  更新しても古い画面が出続ける事故が起きやすいので今は入れていない
- **入れ直さなくても更新は反映される。** push すれば次に開いたときに新しくなる
- iPhoneとPCで別々に追加する必要がある。データはGoogleカレンダー連携後に共有される

---

## 手順2：Googleの設定

<https://console.cloud.google.com/> を開く。Googleアカウントでログインするだけで使える。

### 2-1. プロジェクトを作る

画面上部のプロジェクト選択 → 「新しいプロジェクト」。名前は何でもいい（例：`my-calendar`）。
作成後、そのプロジェクトが選ばれている状態にする。

### 2-2. Calendar API を有効にする

左メニュー「APIとサービス」→「ライブラリ」→ `Google Calendar API` を検索 → **有効にする**

ここを忘れると、あとで403エラーになる。

### 2-3. OAuth同意画面を作る

左メニュー「APIとサービス」→「OAuth同意画面」

- User Type：**外部**
- アプリ名：何でもいい（例：`My Calendar`）
- ユーザーサポートメール／開発者の連絡先：自分のメールアドレス

### 2-4. スコープとテストユーザー

- **スコープ**：`https://www.googleapis.com/auth/calendar.events` を追加
  （カレンダーの予定を読み書きする権限。カレンダーの作成・削除の権限は要求しない）
- **テストユーザー**：**自分のGoogleアカウントを追加する**

テストユーザーの追加を忘れると、自分のアプリなのに自分がログインできない。ここが一番ハマる。

### 2-5. クライアントIDを作る

左メニュー「APIとサービス」→「認証情報」→「認証情報を作成」→「OAuth クライアント ID」

- アプリケーションの種類：**ウェブ アプリケーション**
- **承認済みの JavaScript 生成元**：手順1のURLの、**ドメイン部分だけ**

```
✅ https://<ユーザー名>.github.io
❌ https://<ユーザー名>.github.io/<リポジトリ名>/     ← パスを含めると弾かれる
❌ https://<ユーザー名>.github.io/                     ← 末尾のスラッシュも不要
```

- **承認済みのリダイレクトURI**：**空のままでいい**
  （このアプリはリダイレクトを使わない方式でログインするため）

作成すると **クライアントID** が表示される。`〜.apps.googleusercontent.com` という長い文字列。これをコピー。

> クライアントシークレットも一緒に表示されるが、**こちらは使わないし、使ってはいけない**。
> ブラウザで動くアプリのコードは誰でも読めるので、シークレットを書き込むと漏れる。

---

## 手順3：コードに貼る

`index.html` の上のほう、`CONFIG` を探す。

```js
const CONFIG = {
  googleClientId: '',        // ← ここにさっきのクライアントIDを貼る
  calendarId: 'primary',
  scope: 'https://www.googleapis.com/auth/calendar.events',
};
```

貼ったら push する。GitHub Pages なら1〜2分で反映される。

`calendarId` を `primary` のままにすると、普段使っているGoogleカレンダーに直接入る。
学校の予定だけ分けたいなら、Googleカレンダー側で新しいカレンダーを作って、その設定画面にある
「カレンダーID」をここに貼る。

---

## 手順4：つないでみる

1. 公開したURLを開く
2. 画面下の「**Googleカレンダーとつなぐ**」を押す
3. Googleのログイン画面が出る
4. **「このアプリは確認されていません」と警告が出る** → 「詳細」→「(アプリ名)に移動」で進む
   - 自分しか使わないアプリなので、Googleの審査を通していないため。正常な表示
5. カレンダーへのアクセスを許可
6. 「保存先：Googleカレンダー」に変わればOK

**確認のしかた**：アプリで予定を1件追加して、スマホのGoogleカレンダーアプリを開く。
そこに出ていれば成功。逆にGoogleカレンダー側で予定を足して、アプリを再読み込みして出れば往復できている。

---

## つまずいたら

| 症状 | 原因 |
|---|---|
| `redirect_uri_mismatch` / `origin mismatch` | 手順2-5のJavaScript生成元が違う。パスや末尾スラッシュを消してドメインだけにする |
| `access_denied` | テストユーザーに自分を追加していない（手順2-4） |
| 403 が返る | Calendar API を有効にしていない（手順2-2） |
| 「つなぐ」を押しても何も起きない | ポップアップがブロックされている。アドレスバー右のブロック表示から許可する |
| `googleClientId が空` と出る | 手順3が反映されていない。GitHub Pages の反映待ちか、pushし忘れ |

---

## 連携したあとに改造するとき

コードはいつでも直していい。push すれば1〜2分で反映される。
**Googleの設定をやり直す必要があるのは次の2つだけ。**

- **URLが変わったとき**（リポジトリ名の変更、別サービスへの引っ越し、独自ドメイン）
  → 手順2-5の「承認済みのJavaScript生成元」に新しいドメインを追加する
- **もっと強い権限が要るとき**（いまは予定の読み書きのみ）
  → 同意画面のスコープを変更し、許可を取り直す

見た目の変更、`DAY_START` / `HOUR_PX` / `PERIODS` の調整、機能追加は、Googleの設定に影響しない。

### リスクの性質が変わることに注意

連携前：失敗しても消えるのはこの端末のテストデータだけ。
連携後：**本物のGoogleカレンダーを直接触る。** アプリで予定を消すとGoogleカレンダーからも消える。

- ブランチを切って、`node test.js` を通してからマージする
- `toGoogle()` / `fromGoogle()` を触るときは特に慎重に。日付が1日ズレるだけで実害が出る

### 実験用カレンダーを用意しておくと安全

1. Googleカレンダーで新しいカレンダーを作る（例：`カレンダー開発用`）
2. そのカレンダーの設定画面を開き、「カレンダーの統合」にある**カレンダーID**をコピー
3. 開発中のブランチだけ `CONFIG.calendarId` をそのIDに変える

これで本番の予定を巻き込まずに試せる。動作確認が済んだら `primary` に戻してマージする。

---

## 知っておくこと

**Googleカレンダーが本体になる。**
つないだあとは、予定の実体はGoogle側にある。このアプリはその表示・編集用の画面という位置づけ。
だからスマホの標準カレンダーアプリにも同じ予定が出るし、通知もそちらで鳴る。

**リアルタイム更新はしない。**
他の端末での変更は、ページを再読み込みしたときに反映される。

**ログインは1時間ほどで切れるが、自動で取り直す。**
時間が空くと一瞬待たされることがある。それは正常。

**ローカル保存のデータは自動で移らない。**
つなぐ前に入れた予定をGoogleへ移したいときは、ローカル状態で「データを書き出す」→ Googleにつないでから「データを読み込む」。

**授業は「毎週くり返す1件の予定」として保存される。**
Googleカレンダー側でくり返しルールを複雑に変える（隔週、複数曜日など）と、このアプリからは読めなくなる。

**締切は終日の予定として保存される。**
Google純正アプリからは普通の終日予定に見える。締切かどうかの情報はこのアプリだけが持っている隠し欄に入っているので、
Google側でタイトルを変えるぶんには消えないが、予定を作り直すと締切ではなく普通の予定になる。
